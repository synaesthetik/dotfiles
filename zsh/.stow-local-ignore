# This file REPLACES Stow's built-in default ignore list for this package
# (it does not merge with it). If this package ever gains a README/LICENSE/
# COPYING file, those must be listed here explicitly too, or they will be
# symlinked into $HOME (Stow's built-in defaults are discarded once a
# .stow-local-ignore exists — see 01-RESEARCH.md Pitfall 3).
\.DS_Store
# *.example stubs (e.g. zshrc.local.example) are in-repo reconstruction
# references, not $HOME config — never symlink them out.
.*\.example
